<section id="6" title="Requirements Summary">
  <description>
- List all core features (functional requirements) and non-functional requirements for the MVP.
- Each core feature is assigned a unique ID (F1, F2, …), and will be mapped to Section 7.
- Prioritize each requirement using: Must-Have, Should-Have, or Nice-to-Have.
- Only features defined in this section are included in the MVP scope; additional features must be listed in Section 9.
- Identify a top-3 NFR focus set for MVP prioritization. This is not a deletion cap: retain every mandatory security, privacy, regulatory, accessibility, contractual, and Must-Have constraint even when the final list exceeds three. Optional constraints outside the focus set may be merged or deferred via Section 9.
  </description>
  <subsection id="6.1" title="Core Features (Functional Requirements)">
    <example>
| ID | Feature | Priority |
|----|---------|----------|
| F1 | Email Sign-up | Must-Have |
| F2 | Email Login | Must-Have |
| F3 | Main Screen | Must-Have |
| F4 | Post Creation | Should-Have |
| F5 | Post List/Detail | Nice-to-Have |
    </example>
  </subsection>
  <subsection id="6.2" title="Non-Functional Requirements">
    <description>
- Define non-functional requirements such as security, performance, and scalability.
- Set the minimum standards for the MVP phase.
- Every NFR MUST be REPRODUCIBLE — a measurable threshold plus how it is checked. Reject vague qualifiers ("minimum security", "fast", "scalable"): two readers must not be able to interpret it differently. If you can't state a number or a yes/no condition, it isn't an NFR yet.
- Each NFR's threshold maps 1:1 to a Success Threshold in Section 8 (or to a release test) — that is where it is validated. Keep the two consistent.
- Mark each NFR's Scope: `Global` (applies to the whole system) or a Feature ID (`F3`) it constrains. This scope is what carries the NFR into the ADR cycle as a Decision Driver for that feature.
    </description>
    <example>
| ID | Requirement (measurable) | Scope | Priority |
|----|--------------------------|-------|----------|
| NF1 | Authentication required; passwords stored hashed (no plaintext). Email verification out of MVP scope. | F1, F2 | Must-Have |
| NF2 | Serve 1,000 daily active users while meeting the NF3 response-time requirement | Global | Should-Have |
| NF3 | API responses return in under 3 seconds (p95) | Global | Must-Have |
    </example>
  </subsection>
  <subsection id="6.3" title="Feature Dependency Diagram">
    <description>
- After completing 6.1 and 6.2, the AI automatically analyzes feature dependencies and generates this diagram.
- Visualize the dependency relationships between features using a Mermaid diagram.
- Use a top-down tree structure (graph TD) to show which features depend on other features.
- An arrow from A to B means "A depends on B" (B must be implemented before A).
- Features with no dependencies are root nodes in the tree.
- This diagram helps determine the implementation order and identify critical paths.
- Present the auto-generated diagram to the user for confirmation or correction.
    </description>
    <example>
```mermaid
graph TD
    F1[F1: Email Sign-up]
    F2[F2: Email Login]
    F3[F3: Main Screen]
    F4[F4: Post Creation]
    F5[F5: Post List/Detail]

    F2 -->|depends on| F1
    F3 -->|depends on| F2
    F4 -->|depends on| F2
    F5 -->|depends on| F3
```
    </example>
  </subsection>
</section>
